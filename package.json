{
  "name": "ionic-stencil-capacitor-kitchensink",
  "private": true,
  "version": "0.0.1",
  "description": "ionic-stencil-capacitor-kitchensink",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch"
  },
  "dependencies": {
    "@capacitor/cli": "^1.0.0",
    "@capacitor/core": "^1.0.0",
    "@ionic/core": "one"
  },
  "devDependencies": {
    "@stencil/core": "1.0.7"
  }
}
